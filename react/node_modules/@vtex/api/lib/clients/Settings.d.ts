import { AppClient, InstanceOptions } from '../HttpClient';
import { IOContext } from '../service/typings';
import { AppMetaInfo } from './Apps';
export interface SettingsParams {
    merge?: boolean;
    files?: string[];
}
export declare class Settings extends AppClient {
    constructor(context: IOContext, options?: InstanceOptions);
    getFilteredDependencies(appAtMajor: string, dependencies: AppMetaInfo[]): AppMetaInfo[];
    getDependenciesHash(dependencies: AppMetaInfo[]): string;
    getSettings(dependencies: AppMetaInfo[], appAtMajor: string, params?: SettingsParams): Promise<any>;
}
