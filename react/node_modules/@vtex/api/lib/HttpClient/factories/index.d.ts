import { IOContext } from '../../service/typings';
import { HttpClient } from '../HttpClient';
import { InstanceOptions } from '../typings';
export declare const rootURL: (service: string, { region }: IOContext, { baseURL }: InstanceOptions) => string;
export declare const workspaceURL: (service: string, context: IOContext, opts: InstanceOptions) => string;
export declare const forWorkspace: (service: string, context: IOContext, opts: InstanceOptions) => HttpClient;
export declare const forRoot: (service: string, context: IOContext, opts: InstanceOptions) => HttpClient;
export declare const forExternal: (baseURL: string, context: IOContext, opts: InstanceOptions) => HttpClient;
