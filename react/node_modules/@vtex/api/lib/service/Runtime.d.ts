import { IOClients } from '../clients/IOClients';
import { EnvMetric, MetricsAccumulator } from '../metrics/MetricsAccumulator';
import { Service } from './Service';
import { RouteHandler, ServiceDescriptor } from './typings';
export declare class Runtime<ClientsT extends IOClients = IOClients, StateT = void, CustomT = void> {
    routes: Record<string, RouteHandler<ClientsT, StateT, CustomT>>;
    events: any;
    statusTrack: () => EnvMetric[];
    __is_service: true;
    constructor(service: Service<ClientsT, StateT, CustomT>, descriptor: ServiceDescriptor);
}
declare global {
    namespace NodeJS {
        interface Global {
            metrics: MetricsAccumulator;
        }
    }
}
