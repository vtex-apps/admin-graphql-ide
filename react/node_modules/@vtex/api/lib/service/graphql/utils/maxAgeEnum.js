"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const ramda_1 = require("ramda");
/*
 * This file contains the enumeration -> int map for generating the max age
 * in the graphql cacheControl
 */
exports.maxAgeEnums = {
    LONG: 86400,
    MEDIUM: 3600,
    SHORT: 120,
};
exports.defaultMaxAgeFromCtx = (ctx) => {
    const forwarded = ramda_1.path(['request', 'header', 'x-forwarded-path'], ctx);
    const isPrivateRoute = forwarded && /_v\/graphql\/private\/v*/.test(forwarded);
    return (isPrivateRoute) ? 0 : exports.maxAgeEnums.LONG;
};
