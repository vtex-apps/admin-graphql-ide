import DataLoader from 'dataloader';
import { Messages } from '../../../clients';
import { IOMessage } from '../schema/typeDefs/ioMessage';
export declare const messagesLoader: (messagesAPI: Messages) => DataLoader<IOMessage, string>;
